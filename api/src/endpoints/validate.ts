var express = require("express");
var router = express.Router();

router.post('/validate/username', function(_req: any, _res: any) {
});

router.post('/validate/email', function(_req: any, _res: any) {
});

module.exports = router;
