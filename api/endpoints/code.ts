var express = require("express");
var router = express.Router();

router.get("/code/:id", function(_req: any, _res: any) {
});

router.post('/code/new', function(_req: any, _res: any) {
});

router.post('/code/edit', function(_req: any, _res: any) {
});

module.exports = router;
